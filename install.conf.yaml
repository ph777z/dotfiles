- link:
    ~/.config: 
      glob: true
      path: dotfiles/config/*
    ~/.zshrc: dotfiles/.zshrc
    ~/.gitconfig: dotfiles/.gitconfig
    ~/.tmux.conf: dotfiles/.tmux.conf

- shell:
  - scripts/config.sh

- clean: ['~', '~/.config']